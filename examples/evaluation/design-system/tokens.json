{
  "$description": "The design system's allowed tokens, in DTCG format. In a real pipeline these come from your token source via Style Dictionary; here they mirror examples/app/src/tokens.ts. Raw values are intentionally un-normalized (mixed-case hex, a named color) — the ETL normalizes them with @factlas/core so they compare against facts on identical footing.",
  "color": {
    "brand": { "$type": "color", "$value": "#3366FF" },
    "brand-hover": { "$type": "color", "$value": "#2851d6" },
    "danger": { "$type": "color", "$value": "#E00" },
    "success": { "$type": "color", "$value": "green" }
  },
  "space": {
    "sm": { "$type": "dimension", "$value": "4px" }
  },
  "radius": {
    "md": { "$type": "dimension", "$value": "8px" }
  }
}
